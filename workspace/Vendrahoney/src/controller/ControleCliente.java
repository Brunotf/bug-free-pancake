package controller;

import model.Cliente;
import java.util.LinkedList;

public class ControleCliente {
	
	public boolean verificaCpf(Cliente cpf) {
		boolean verificador = false;
		return verificador;
	}
	
	public LinkedList<Cliente> consultaAnimal(Cliente animal) {
		LinkedList<Cliente> lista = new LinkedList<Cliente>();
		lista.add(animal);
		return lista;
	}
	
}
